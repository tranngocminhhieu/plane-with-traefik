# ── Toàn bộ thư mục của Plane (nhà phát triển gốc) ──
# setup.sh tự tải về, repo này không giữ bản sao nào.
setup.sh
plane-app/

# ── Secret ──
plane.local.env

# ── Backup ──
backups/

# ── Công cụ cá nhân ──
.claude/
